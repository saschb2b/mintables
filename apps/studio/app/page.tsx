/**
 * The hub route. Desktop icons, wallpaper, dock, and menu bar all live in
 * providers and persist across every route, so this page renders nothing of
 * its own. Visiting `/` just means "no app window is the URL anchor".
 */
export default function HubPage() {
  return null;
}
