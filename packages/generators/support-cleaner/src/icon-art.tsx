import { DockIconImage } from "@mintables/shared/ui";

export function SupportCleanerIconArt({ size = 34 }: { size?: number }) {
  return <DockIconImage src="/app-icons/support-cleaner.png" size={size} />;
}
